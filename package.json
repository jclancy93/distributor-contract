{
  "name": "distributor-contract",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "compile": "oz compile",
    "compile-all": "npm run compile && npm run nxm-compile && npm run nxm-copy-artifacts",
    "nxm-compile": "(cd nexusmutual-contracts && npx truffle compile)",
    "nxm-copy-artifacts": "cp -n nexusmutual-contracts/build/contracts/*.json build/contracts/",
    "dev": "nodemon --ext js,sol -w contracts -w test -x 'oz compile && mocha --exit'",
    "test": "mocha --exit"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NexusMutual/distributor-contract.git"
  },
  "author": "tech@nexusmutual.io",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/NexusMutual/distributor-contract/issues"
  },
  "homepage": "https://github.com/NexusMutual/distributor-contract#readme",
  "dependencies": {
    "@nexusmutual/cover-api": "^0.0.1",
    "@openzeppelin/cli": "^2.8.0",
    "@openzeppelin/contracts": "^2.5.0"
  },
  "devDependencies": {
    "@truffle/hdwallet-provider": "^1.0.34",
    "@openzeppelin/test-environment": "^0.1.3",
    "@openzeppelin/test-helpers": "^0.5.4",
    "chai": "^4.2.0",
    "eslint": "^6.8.0",
    "mocha": "^7.1.0"
  }
}
